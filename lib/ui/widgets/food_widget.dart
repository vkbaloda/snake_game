import 'package:flutter/material.dart';

class SimpleFoodWidget extends StatelessWidget {
  const SimpleFoodWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.sports_baseball);
  }
}
